RewriteEngine On

# Redirect requests for .html files to URLs without .html extension
RewriteCond %{THE_REQUEST} /\s([^.]+)\.html [NC]
RewriteRule ^ %1 [R=301,L]

# Internally map URLs without .html to .html files
RewriteCond %{REQUEST_FILENAME}.html -f
RewriteRule ^(.*)$ $1.html [L]
